= title "Widgets"
- content_for :sidebar do
  = link_to "New Widget", new_widget_path, :class => 'button icon add'
%ul.widgets
  - @widgets.each do |widget|
    %li= link_to( image_tag( widget.image(:thumb) ) , widget_path(widget) )
