= title "Theme Package: #{@theme_package.name}"
%h3 Add a widget to your theme package

%ul.widgets
  - @widgets.each do |widget|
    %li= link_to(image_tag(widget.image.url(:thumb)), add_widget_theme_package_path(:theme_package_id => @theme_package.id, :widget_id => widget.id))
